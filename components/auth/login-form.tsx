// Auth disabled for initial deploy. This component is kept as a placeholder so
// the import graph stays valid; it will be rewritten against Better Auth in
// the next stage.
export function LoginForm() {
  return null;
}
